# Guía de contribución

1. Haz un fork y crea una rama desde `main`.
2. Sigue el estándar de commits de `docs/commit-standard.md`.
3. Instala los hooks con `pre-commit install`.
4. Ejecuta `Invoke-Pester ./tests` antes de abrir el Pull Request.
5. Abre el PR usando la plantilla incluida.
